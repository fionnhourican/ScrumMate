# Production secrets (use external secret management in production)
# WARNING: Replace all values with secure secrets before deployment
scrummate:
  secrets:
    database:
      username: ""  # Set via external secret management
      password: ""  # Set via external secret management
    jwt:
      secret: ""    # Set via external secret management (minimum 32 characters)
    registry:
      dockerconfigjson: ""  # Set via external secret management

postgresql:
  auth:
    password: ""  # Set via external secret management

monitoring:
  grafana:
    adminPassword: ""  # Set via external secret management
