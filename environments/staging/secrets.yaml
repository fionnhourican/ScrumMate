# Staging secrets (replace with actual values)
scrummate:
  secrets:
    database:
      username: scrummate_staging
      password: CHANGE_THIS_STAGING_PASSWORD
    jwt:
      secret: CHANGE_THIS_STAGING_JWT_SECRET_KEY_MINIMUM_32_CHARACTERS
    registry:
      dockerconfigjson: ""

postgresql:
  auth:
    password: CHANGE_THIS_STAGING_PASSWORD

monitoring:
  grafana:
    adminPassword: CHANGE_THIS_GRAFANA_PASSWORD
